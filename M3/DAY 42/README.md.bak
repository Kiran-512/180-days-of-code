STACK DS:

- Ordered DS
- TOP/TOS
- TOS : Top of stack
- LIFO : Last In First Out

Operations on stack
-------------------------
- Insertion : push 
- Deletion : pop
- isEmpty(): to check if the stack is empty or not
- isFull() : to check if the stack is full or not
- peek() : to return CURRENT POSTION of the TOS
- count() :
- change() :
- display() :

Stack Applications


Polish Notations :
-------------------------
Infix Notations => Operator in between operands
Prefix Notations => Operator at the begining of expression
Postfix Notations => Operator at the end of expression
 
BODMAS:
- (){}[]      L-R
- ^           R-L 
- */ 		  L-R
- +- 		  L-R

Conversion of infix to postfix:
----------
A+(B/C)-D => A+(BC/)-C => (A(BC/)+)-C => ((A(BC/)+)D)-

Conversion of infix to prefix:
----------
A+(B/C)-D => A+(/BC)-D => +A/BC-D => -+A/BCD

eg2.
(A+B)/(C-D)
Pre => /+AB-CD
Post=> AB+CD-/

eg3.
A+B/C-D
Pre => -+A/BCD
Post=> ABC/+D-

eg4.
A+B-C/D-E^F*G

NOTE: For this topic precedence and associativity of the operator is must know 
